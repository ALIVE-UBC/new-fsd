# Database Configuration
# Update these values to match your phpMyAdmin database settings

DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=your_database_name

# If your database is on a different machine, replace host.docker.internal with the actual IP/hostname
# Example: DB_HOST=192.168.1.100 or DB_HOST=your-server.com 